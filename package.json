{
  "name": "@arcjr/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "serve": "turbo serve",
    "start": "turbo start",
    "lint": "turbo lint",
    "fmt": "turbo fmt",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "packageManager": "bun@1.3.2",
  "engines": {
    "bun": ">=1.3.2",
    "node": ">=24"
  },
  "dependencies": {
    "dompurify": "^3.3.1"
  }
}