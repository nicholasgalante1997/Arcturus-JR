import { Posts } from '@/components/Posts';
import { AppLayout } from '@/layout/Layout';

function PostsPage() {
  return (
    <AppLayout>
      <Posts />
    </AppLayout>
  );
}

export default PostsPage;
