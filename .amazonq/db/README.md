# Amazon Q CLI Sessions Save States

This directory contains saved states for Amazon Q CLI sessions. These files are used by the Amazon Q Developer CLI to maintain context and state across different interactions with the CLI agents.

Each file corresponds to a specific CLI agent and includes information such as the agent's configuration, prompts, and any relevant context needed for its operation.
