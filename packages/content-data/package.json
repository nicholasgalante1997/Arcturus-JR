{
  "name": "@arcjr/content-data",
  "version": "1.0.0",
  "description": "Source-of-truth content (posts, rfcs, flat pages) for @ArcJr — validates and emits a generated dist/ that apps/web syncs into public/content",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build.ts",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "typescript": "latest"
  },
  "dependencies": {
    "@arcjr/content": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.1",
    "typescript": "^5.0.0"
  }
}
