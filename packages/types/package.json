{
  "name": "@arcjr/types",
  "version": "1.0.0",
  "description": "Shared typescript interfaces, enums, types, and constants for @ArcJr packages",
  "private": true,
  "type": "module",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "run-s bundle check-types",
    "bundle": "echo \"No need to bundle a types only package!\"",
    "bundle:watch": "bun run build.ts --watch",
    "check-types": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "typescript": "latest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.0.0",
    "npm-run-all": "4.1.5",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.0.0"
  }
}
