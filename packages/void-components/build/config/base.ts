import type { BuildOptions } from 'esbuild';
import { getPeerDeps } from '../utils/dependencies';

export const BaseConfig: BuildOptions = {
    banner: {
        js: `/* Void-Components (Arcturus-Jr.v2) | License MIT */`,
        css: `// Do not modify this file directly, the contents of it were autogenerated. Timestamp: ${new Date().toLocaleDateString()}`
    },
    bundle: true,
    sourcemap: true,
    jsx: "automatic",
    platform: "neutral",
    metafile: true,
    external: await getPeerDeps(),
    target: 'esnext'
};